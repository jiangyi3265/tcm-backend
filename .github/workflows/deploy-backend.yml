name: Deploy Production Backend

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: production-backend
  cancel-in-progress: false

jobs:
  build-and-deploy:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - name: Test and build
        run: |
          python3 -m unittest discover -s ops -p 'test_*.py'
          mvn -B --settings .github/maven-settings.xml clean package
      - name: Publish to production
        shell: bash
        env:
          PRODUCTION_DEPLOY_KEY: ${{ secrets.PRODUCTION_DEPLOY_KEY }}
        run: |
          set -euo pipefail
          umask 077
          test -n "$PRODUCTION_DEPLOY_KEY"
          mkdir -p ~/.ssh
          printf '%s\n' "$PRODUCTION_DEPLOY_KEY" | tr -d '\r' > ~/.ssh/production_key
          cp .github/production_known_hosts ~/.ssh/production_hosts
          SHA256=$(sha256sum ruoyi-admin/target/ruoyi-admin.jar | cut -d ' ' -f 1)
          ssh -i ~/.ssh/production_key -o BatchMode=yes -o ConnectTimeout=20 \
            -o ServerAliveInterval=15 -o ServerAliveCountMax=4 \
            -o StrictHostKeyChecking=yes -o UserKnownHostsFile=~/.ssh/production_hosts \
            root@152.136.191.51 "deploy $GITHUB_SHA $SHA256" < ruoyi-admin/target/ruoyi-admin.jar
      - name: Remove deployment key
        if: always()
        run: rm -f ~/.ssh/production_key
